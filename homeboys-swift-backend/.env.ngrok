# Copy values from your main .env or fill these in and rename to .env to apply.
# NGROK_AUTH_TOKEN should be set to your ngrok authtoken (run `ngrok authtoken <token>` to set it globally).
# To reserve a stable public hostname you must have a paid ngrok plan and set NGROK_SUBDOMAIN.
NGROK_AUTH_TOKEN=
NGROK_SUBDOMAIN=
